
{% block body %}
  <h1>Subject</h1>

  <table>
    <tbody>
    <tr>
    {% for id in Subject %}
      
    
      <th>Id</th>
      <td>{{ id.id }}</td>
    </tr>
    <tr>
      <th>Name</th>
      <td>{{ id.name }}</td>
    </tr>
    </tbody>
  </table>

  <ul>
    <li>
      <a href="{{ path('guest') }}">Back to the list</a>
    </li>
  </ul>
{% endblock %}